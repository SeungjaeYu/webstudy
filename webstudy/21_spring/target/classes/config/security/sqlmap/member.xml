<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mlec.security.dao.MemberMapper">
	<resultMap type="member" id="memberMap">
		<id property="id" column="id" />
		<result property="id" column="id" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="reg_date" column="regDate" />
		<collection property="authList" resultMap="authMap" />
	</resultMap>

	<resultMap type="auth" id="authMap">
		<result property="id" column="id" />
		<result property="auth" column="auth" />
	</resultMap>

	<select id="selectMember" parameterType="string" resultMap="memberMap">
		SELECT mem.id, password, name, reg_date, auth
		  FROM tb_security_member mem LEFT OUTER JOIN tb_security_member_auth auth 
		                                           on mem.id = auth.id
		 WHERE mem.id = #{id}
	</select>
	
	<insert id="insertMember" parameterType="member">
	    insert into tb_security_member(id, password, name) values(#{id}, #{password}, #{name})
	</insert>	
	
	<insert id="insertMemberAuth" parameterType="auth">
	    insert into tb_security_member_auth(id, auth) values(#{id}, #{auth})
	</insert>
	
</mapper>
