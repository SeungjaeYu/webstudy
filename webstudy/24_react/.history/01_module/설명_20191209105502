모듈 시스템

- NodeJS에서 사용하는 CommonJS
  : 정의
    module.exports 객체 등록
  : 사용
    require("모듈")

- ECMA 에서 정의한 ESM
  : Node에서 ecma에서 정의한 esm 모듈을 사용하기 위해서는 별도의 설치 필요
    npm install esm
  : 실행시
    node -r esm 파일명

  : 정의
    export 객체
    export default 객체

  : 사용
    import 모듈정의이름 from 모듈
    import 모듈정의이름 as 변경이름 from 모듈


