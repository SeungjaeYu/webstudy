const http = require("http");

module.exports = function () {
    const server;
    return {
        listen(port, fn) {
            server.listen(port, fn);
        },

        get(urlPath, ) {

        },

        post() {

        }
    }
}
